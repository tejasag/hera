import std;

let fizzbuzz = fn(x) {
    if (x % 15 == 0) {
        "FizzBuzz"
    } else if (x % 3 == 0) {
        "Fizz"
    } else if (x % 5 == 0) {
        "Buzz"
    } else {
        x
    }
};

let thirty = range(30);

print(map(thirty, fizzbuzz));
